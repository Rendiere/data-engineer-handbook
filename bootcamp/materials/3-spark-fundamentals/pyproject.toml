[project]
name = "3-spark-fundamentals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest-spark>=0.8.0",
    "pytest>=8.4.1",
    "pyspark>=4.0.0",
]
